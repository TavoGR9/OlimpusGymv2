// carrito.component.ts
import { Component } from '@angular/core';
//import { ProductService } from '../../servicios/';

@Component({
  selector: 'app-carrito',
  templateUrl: './carrito.component.html'
})

export class CarritoComponent {/*
  cart: any[] = [];
  email: string = '';

  constructor(private productService: ProductService) {}

  ngOnInit() {
    // Lógica para cargar el carrito desde el servicio o el almacenamiento local.
    this.cart = this.productService.getCart();
  }

  removeFromCart(productId: number) {
    // Lógica para eliminar un producto del carrito en el servicio.
    this.productService.removeFromCart(productId);
  }

  processSale() {
    // Lógica para procesar la venta en el servicio, usando el valor de 'email'.
    this.productService.processSale(this.email);
  }*/
}